#include "main.h"

/**
* _islower - print_alphabet
* Waiting for output
* @c: wake up
* return: 1 or 0
* if all goes well, then we get a return value
*/

int _islower(int c)
{

	if (c > 96 && c < 123)
	{
		return (1);
	}

	else
	{
		return (0);
	}
}

/**
* Main - check code
*
* Return: Always 0.
*/
